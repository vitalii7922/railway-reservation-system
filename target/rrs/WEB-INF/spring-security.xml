<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:jdbc="http://www.springframework.org/schema/jdbc"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-3.2.xsd http://www.springframework.org/schema/jdbc http://www.springframework.org/schema/jdbc/spring-jdbc.xsd">

    <!-- enable use-expressions -->
    <http auto-config="true" use-expressions="true">
        <!--<intercept-url pattern="/admin/menu.jsp" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/admin/passenger_list.jsp" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/admin/seats.jsp" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/admin/train.jsp" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/trip" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/trips" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/trains" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/seats" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/passengers" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/station" access="hasRole('ROLE_ADMIN')"/>-->
        <intercept-url pattern="/admin/*" access="hasRole('ROLE_ADMIN')"/>
        <!-- access denied page -->

        <form-login login-page="/login.jsp"
                    default-target-url="/admin/menu.jsp"
                    authentication-failure-url="/login?error"
                    username-parameter="username"
                    password-parameter="password"/>
        <logout logout-success-url="/login?logout"/>
        <access-denied-handler error-page="/403"/>
        <!--		<logout logout-url="/logout"/>-->
        <!--		<logout logout-url="/login?logout"/>-->
        <!-- enable csrf protection -->
        <!--		<csrf/>-->
    </http>

    <!-- Select users and user_roles from database -->
    <authentication-manager>
        <authentication-provider>
            <jdbc-user-service data-source-ref="dataSource"
                               users-by-username-query=
                                       "select username,password, enabled from users where username=?"
                               authorities-by-username-query=
                                       "select username, role from user_roles where username =?"/>
        </authentication-provider>
    </authentication-manager>
</beans:beans>
